def safeExtGet(prop, fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

apply plugin: 'com.android.library'
//apply plugin: 'maven'
apply plugin: 'com.mob.sdk'

//group = "com.fengwuxp.reactnative.mob"
//version = "1.0.0"

buildscript {
    repositories {
        maven {
            url "http://mvn.mob.com/android"
        }
        mavenLocal()
        google()
        jcenter()
        maven() {
            url 'http://repo.codeboot.net/maven/'
        }

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.2'
        classpath 'com.mob.sdk:MobSDK:+'
    }
}

android {
    compileSdkVersion safeExtGet('compileSdkVersion', 28)
    buildToolsVersion safeExtGet('buildToolsVersion', '28.0.3')

    defaultConfig {
        minSdkVersion safeExtGet('minSdkVersion', 19)
        targetSdkVersion safeExtGet('targetSdkVersion', 28)
        versionCode 1
        versionName "${version}"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

def MOB_SDK_APP_KEY = safeExtGet('MOB_SDK_APP_KEY', "");
def MOB_SDK_APP_SECRET = safeExtGet('MOB_SDK_APP_SECRET', "");

// 新浪
def SINA_WEI_BO_APP_KEY = safeExtGet('SINA_WEI_BO_APP_KEY', "");
def SINA_WEI_BO_APP_SECRET = safeExtGet('SINA_WEI_BO_APP_SECRET', "");
def SINA_WEI_BO_APP_CALLBACK_URL = safeExtGet('SINA_WEI_BO_APP_CALLBACK_URL', "http://www.sharesdk.cn");
def SINA_WEI_BO_APP_SHARE_BY_APP_CLIENT = safeExtGet('SINA_WEI_BO_APP_SHARE_BY_APP_CLIENT', false);

// 微信
def WE_CHAT_APP_ID = safeExtGet('WE_CHAT_APP_ID', "");
def WE_CHAT_APP_SECRET = safeExtGet('WE_CHAT_APP_SECRET', "");

// 微信朋友圈
def WE_CHANT_MOMENTS_APP_ID = "wx4868b35061f87885"
def WE_CHANT_MOMENTS_APP_SECRET = "64020361b8ec4c99936c0e3999a9f249"

// 微信收藏 Favorite
def WE_CHANT_FAVORITE_APP_ID = "wx4868b35061f87885"
def WE_CHANT_FAVORITE_APP_SECRET = "64020361b8ec4c99936c0e3999a9f249"

// QQ
def QQ_APP_ID = safeExtGet('QQ_APP_ID', "");
def QQ_APP_EKY = safeExtGet('QQ_APP_EKY', "");

// QQ 空间
def QQ_ZONE_APP_ID = safeExtGet('QQ_ZONE_APP_ID', "");
def QQ_ZONE_APP_EKY = safeExtGet('QQ_ZONE_APP_EKY', "");

// 支付宝
def ALI_PAY_APP_ID = safeExtGet('ALI_PAY_APP_ID', "");
def ALI_PAY_APP_KEY = safeExtGet('ALI_PAY_APP_KEY', "");
def ALI_PAY_APP_SECRET = safeExtGet('ALI_PAY_APP_SECRET', "");

MobSDK {
    appKey MOB_SDK_APP_KEY
    appSecret MOB_SDK_APP_SECRET

    ShareSDK {
        //平台配置信息
        devInfo {
            Email {

            }
            SinaWeibo {
                appKey SINA_WEI_BO_APP_KEY
                appSecret SINA_WEI_BO_APP_SECRET
                callbackUri SINA_WEI_BO_APP_CALLBACK_URL
                shareByAppClient SINA_WEI_BO_APP_SHARE_BY_APP_CLIENT
            }
            Wechat {
                appId WE_CHAT_APP_ID
                appSecret WE_CHAT_APP_SECRET
            }
            WechatFavorite {
                appId WE_CHANT_FAVORITE_APP_ID
                appSecret WE_CHANT_FAVORITE_APP_SECRET
            }
            WechatMoments {
                appId WE_CHANT_MOMENTS_APP_ID
                appSecret WE_CHANT_MOMENTS_APP_SECRET
            }
            QQ {
                appId QQ_APP_ID
                appKey QQ_APP_EKY
            }
            QZone {
                appId QQ_ZONE_APP_ID
                appKey QQ_ZONE_APP_EKY
            }
            TencentWeibo {

            }
            AliPay {
                appId ALI_PAY_APP_ID
                appKey ALI_PAY_APP_KEY
                appSecret ALI_PAY_APP_SECRET
            }
            YouDao {

            }
            ShortMessage {

            }
            Linkedin {

            }
            Facebook {

            }
        }
    }

}

dependencies {
    compileOnly 'com.facebook.react:react-native:+'
}

